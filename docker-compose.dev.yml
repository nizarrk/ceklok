version: '3.7'

services:
  dev-ceklok-coreplatform:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - /home/apps/config_env/ceklok/ceklok-core-platform/.env:/usr/src/app/.env
      - /home/apps/logs_pm2:/usr/src/log
      - /home/apps/cdn_image/ceklok-core-platform:/usr/src/app/public
    ports:
      - 3012:3012
    tty: true
    container_name: dev-ceklok-coreplatform
    image: gitlab.vascomm.co.id:4567/ceklok/ceklok-core-platform:latest_dev
    networks:
      - vascomm-app
    restart: always

networks:
  vascomm-app:
    name: vascomm-app
    driver: bridge
